---
status: accepted
---

# 由宿主在启动时提供前端运行配置

Web 与桌面共用同一份静态前端产物，因此 API 地址、版本和可用能力不编译进 `NEXT_PUBLIC_*` 变量。前端在挂载业务界面前统一加载运行时配置：Web 读取部署时生成的配置，Tauri 在启动 FastAPI sidecar 后通过宿主桥接提供动态配置；业务组件只依赖已初始化的统一传输层，不直接读取宿主或构建环境变量。这样既保持单一产物，又让不同部署环境和桌面动态端口保持可控。
