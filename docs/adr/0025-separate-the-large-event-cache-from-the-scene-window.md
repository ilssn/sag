# 将大容量事件缓存与场景窗口分离

知识宇宙使用默认可容纳 1000 个事项的纯数据 FIFO 缓存，以支持长时间探索和快速回退，并允许用户在设置中调整容量；只有知识宇宙窗口内的事项及其相关实体会被投影为场景节点、连线和卡片。缓存不分配 Three.js 对象、DOM 或 GPU 资源；达到容量后按进入缓存的先后淘汰最旧记录，不引入 LRU、关系权重、桥接保护或其他隐式优先级。当前窗口持有不可变事项引用，因此缓存索引淘汰不会让正在展示的节点突然消失。
