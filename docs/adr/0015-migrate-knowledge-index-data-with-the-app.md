---
status: accepted
---

# 知识索引数据必须随应用迁移保护

zleap-sag 的 SQLite、LanceDB 及事件实体索引属于生成成本较高的持久数据，不视为可在升级时删除的缓存。桌面发行版精确锁定 zleap-sag 版本，并在数据目录记录独立的引擎数据版本；每个客户端版本必须声明并验证可支持的升级路径，存储结构变化必须随版本提供迁移程序。缺少迁移路径时阻止发布该升级，只有用户主动选择重建索引时才允许重新处理原始文档。
