---
status: accepted
---

# 应用托管文件只保存相对存储键

文档与附件在数据库中只保存相对于应用数据目录的存储键，所有磁盘访问统一通过存储层解析并校验结果仍位于数据根目录内。现有 `Document.storage_path` 的绝对路径语义直接替换为 `storage_key`，不在运行时长期维护新旧两套读取分支；已有开发数据通过一次性迁移工具转换。该选择使数据目录迁移、备份恢复和 macOS/Windows 之间的数据转移不受原机器路径约束。
