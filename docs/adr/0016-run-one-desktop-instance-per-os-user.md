---
status: accepted
---

# 每个系统用户只运行一个桌面主实例

SAG 桌面版按系统用户保持单实例，只有主实例可以持有应用数据目录锁、SQLite 连接、FastAPI sidecar 和稳定的本机 API/MCP 端口。再次启动应用时把请求交给现有实例并显示、聚焦主窗口，不创建第二套业务进程。自托管服务仍可独立运行，但必须使用不同的数据目录和端口，不能与活动桌面实例并发写入同一套数据。
