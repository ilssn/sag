---
status: accepted
---

# 关闭桌面主窗口后默认继续后台运行

为避免外部宿主使用的本机 API/MCP 随主窗口关闭而中断，macOS 与 Windows 关闭主窗口时默认只隐藏界面，托盘或菜单栏入口与 FastAPI sidecar 继续运行；只有显式退出 SAG 才停止服务。用户可以把关闭行为改为直接退出。该生命周期能力不引入独立宠物形态，但保证两端行为一致且仍由用户控制。
