---
status: accepted
---

# 桌面本机集成使用稳定且可配置的回环端点

桌面 V1 的 API/MCP 主机固定为 `127.0.0.1`，MCP 路径固定为 `/mcp/`，端口使用公开默认值并允许用户修改、持久化。连接地址由应用生成，而不是让用户任意填写完整 URL；端口冲突时显式要求修复，不静默改用随机端口。该选择让外部宿主保存的配置跨重启稳定，同时保持本地服务只监听回环地址；局域网和公网监听继续由自托管部署承担。
