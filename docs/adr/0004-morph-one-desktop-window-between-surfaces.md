---
status: proposed
---

# 单一桌面窗口在主面板与伴随形态间变换

该方案是首版之后桌面伴随里程碑的候选窗口拓扑，不属于桌面 V1。届时可只创建一个持久的 Tauri WebView 与原生宿主窗口，并在主面板形态和桌面伴随形态之间动态切换窗口属性；选择单窗口变形，是因为两种客户端形态互斥，且同一 React 运行时可以直接保留现有共享 Provider、草稿和进行中任务。实现前仍需针对 macOS 与 Windows 验证窗口属性、透明区域、跨屏幕和缩放变化，因此该拓扑保持提案状态。
