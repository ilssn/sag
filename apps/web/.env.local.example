# 前端不再读取任何 NEXT_PUBLIC_* 编译期变量（ADR-0007：运行时配置）。
#
# 本地开发想覆盖 API 地址时，在 public/config.json（已 gitignore）写入：
#   { "apiBase": "http://192.168.x.x:8000", "enableWindowScaling": true }
# 留空 apiBase 或删除该文件 = 自动推导：
#   - localhost 访问 → http://localhost:8000
#   - 局域网 IP 访问 → 同主机 :8000
#
# 生产 web 部署由容器入口按 SAG_WEB_API_BASE 生成 config.json；
# 桌面壳通过 window.__SAG_RUNTIME_CONFIG__ 注入，均无需本文件。
